# ===========================================
# SECURITY CONFIGURATION
# ===========================================
# IMPORTANT: Never commit .env files to version control
# IMPORTANT: Rotate all keys immediately if .env is exposed

# ===========================================
# Supabase Configuration
# ===========================================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Server-only Supabase key - NEVER prefix with NEXT_PUBLIC_
# This key has admin access to your database
SUPABASE_SERVICE_ROLE_KEY=

# ===========================================
# JWT Secret for Authentication
# ===========================================
# SECURITY: Generate a cryptographically secure secret
# Run: openssl rand -base64 32
# NEVER use human-readable strings or demo values
JWT_SECRET=

# ===========================================
# OpenAI Configuration
# ===========================================
OPENAI_API_KEY=

# ===========================================
# AI Service Configuration
# ===========================================
# SECURITY: Use HTTPS in production
AI_SERVICE_URL=https://ai-service.example.com
# SECURITY: Generate a secure API key: openssl rand -hex 32
AI_SERVICE_API_KEY=
# IP whitelist for AI service (comma-separated)
AI_SERVICE_ALLOWED_IPS=127.0.0.1,::1
WS_BASE_URL=ai.internal.example.com

# ===========================================
# SSL Certificate (Optional, for self-signed certs)
# ===========================================
# Path to CA certificate file for AI service connections
# AI_SERVICE_CA_CERT=/path/to/ca-certificate.pem
