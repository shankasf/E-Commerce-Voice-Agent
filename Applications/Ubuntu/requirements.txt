# Core dependencies
websockets>=12.0
aiohttp>=3.9.0
PyJWT>=2.8.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# System monitoring
psutil>=5.9.0

# GUI dependencies (optional - auto-detected)
PyQt6>=6.6.0; sys_platform == 'linux'
PyQt6-Qt6>=6.6.0; sys_platform == 'linux'

# CLI enhancement
rich>=13.7.0
click>=8.1.7
prompt-toolkit>=3.0.43

# Configuration
pyyaml>=6.0.1

# Logging
python-json-logger>=2.0.7

# HTTP client
httpx>=0.26.0

# Cryptography
cryptography>=41.0.0

# D-Bus for systemd integration (Linux only)
# Note: Install system packages instead: sudo apt-get install python3-gi python3-dbus
# PyGObject>=3.42.0; sys_platform == 'linux'
# dbus-python>=1.3.2; sys_platform == 'linux'

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.12.0
flake8>=6.1.0
pylint>=3.0.0
mypy>=1.7.0
