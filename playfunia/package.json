{
  "name": "webhook",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "start:forever": "pm2 start server.js --name callsphere-gateway",
    "stop": "pm2 stop callsphere-gateway",
    "restart": "pm2 restart callsphere-gateway",
    "logs": "pm2 logs callsphere-gateway",
    "status": "pm2 status"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "body-parser": "^2.2.1",
    "dotenv": "^16.6.1",
    "express": "^5.2.1",
    "http-proxy-middleware": "^3.0.5",
    "twilio": "^5.10.7",
    "ws": "^8.18.3"
  }
}
