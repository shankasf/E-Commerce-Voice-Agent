apiVersion: v1
kind: Service
metadata:
  name: agentic-cloud
  namespace: agentic-cloud
  labels:
    app.kubernetes.io/name: agentic-cloud
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: agentic-cloud
